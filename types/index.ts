export interface SkillCategory {
  title: string;
  description: string;
}

export interface FeaturedProject {
  slug: string;
  title: string;
  description: string;
}

export interface NavLink {
  label: string;
  href: string;
}
